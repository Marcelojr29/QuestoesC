// Olhe a figura abaixo e explique o que é o %c?
int main()
{
    char Ch;

    Ch = D;

    printf("%c", Ch); // No código fornecido, `%c` é um especificador de formato utilizado na função printf para exibir um caractere.
    // No exemplo dado, a variável `Ch` é do tipo `char`, que representa um único caractere.

    return 0;
}